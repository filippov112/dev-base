#lang #lang-python #flask

---
#### app.py
```python

from flask import Flask, request, render_template

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/process', methods=['POST'])
def process():
    user_input = request.form['user_input']
    # Perform some actions with the user_input, for example, reversing the input string
    result = user_input[::-1]
    return render_template('index.html', user_input=user_input, result=result)

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=True)

```

#### templates/index.html
```html
<!--  -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Flask Form Example</title>
  </head>
  <body>
    <div class="container">
      <h1>Enter your input</h1>
      <form method="POST" action="/process">
        <div class="form-group">
          <label for="user_input">Input:</label>
          <input type="text" class="form-control" id="user_input" name="user_input" required>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
      {% if user_input %}
      <h2>Result</h2>
      <p>Input: {{ user_input }}</p>
      <p>Output: {{ result }}</p>
      {% endif %}
    </div>
  </body>
</html>

```

#### Запуск:
```bash
python app.py
```